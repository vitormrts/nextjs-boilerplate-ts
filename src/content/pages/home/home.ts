import { HomeProps } from "./home.types";

const content: HomeProps = {
  title: "Welcome to the Home Page",
  description:
    "To see this content, go to src > locale > pages > home > home.ts",
};

export default content;
